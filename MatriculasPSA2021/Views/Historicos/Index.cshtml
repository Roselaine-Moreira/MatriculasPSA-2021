@model IEnumerable<Entidades.Models.Historico>

@{
    ViewData["Title"] = "Index";
}

<h1>Histórico do Aluno</h1>

@*<p>
    <a asp-action="Create">Create New</a>
</p>*@
<table class="table">
    <thead>
        <tr>

            <th>
                @Html.DisplayNameFor(model => model.AnoSemetre)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.Disciplina)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nota)
            </th>
           
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.AnoSemetre)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Disciplina.NomeDisciplina)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nota)
            </td>
            @*<td>
            <a asp-action="Edit" asp-route-id="@item.HistoricoId">Edit</a> |
            <a asp-action="Details" asp-route-id="@item.HistoricoId">Details</a> |
            <a asp-action="Delete" asp-route-id="@item.HistoricoId">Delete</a>
        </td>*@
        </tr>
}
    </tbody>
</table>
